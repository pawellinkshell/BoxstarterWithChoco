# My own boxstarter setup

## Preparation 
### Install Boxstarter & Chocolatey
  1) Run `Windows PowerShell` with admin privileges.
  2) Go to `Boxstarter` folder
  3) Run script `setup.bat` to install `Boxstarter` with `Chocolatey`